name: build-package

on:
  workflow_dispatch:

  repository_dispatch:
    types:
      - webhook
      - '**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v4

      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
      
